using AMCommunicator;

namespace ArtemisManagerAction
{
    public static class TakeArtemisAction
    {
        public static bool ProcessArtemisAction(ActionCommands action)
        {
            bool WasProcessed = false;
            return WasProcessed;
        }
    }
}